require('dotenv').config();const fs=require('fs');const path=require('path');const {pool}=require('./db');
(async()=>{try{for(const file of ['001_init.sql','002_admin.sql']){await pool.query(fs.readFileSync(path.join(__dirname,'..','sql',file),'utf8'));console.log(`[DB] Applied ${file}`);}console.log('[DB] Migration complete.');}finally{await pool.end();}})().catch(err=>{console.error('[DB] Migration failed:',err.message);process.exit(1);});
