require('dotenv').config();
const bcrypt=require('bcryptjs');const {pool}=require('./db');
(async()=>{const [username,password,email]=process.argv.slice(2);if(!username||!password){console.error('Usage: npm run create-admin -- <username> <strong-password> [email]');process.exit(1);}if(password.length<12){console.error('Use an admin password of at least 12 characters.');process.exit(1);}try{const hash=await bcrypt.hash(password,12);const q=await pool.query(`INSERT INTO users(username,email,password_hash,full_name,role,status) VALUES($1,$2,$3,$4,'superadmin','active') ON CONFLICT(username) DO UPDATE SET password_hash=EXCLUDED.password_hash,email=COALESCE(EXCLUDED.email,users.email),role='superadmin',status='active',updated_at=now() RETURNING id,username,email,role,status`,[username,email||null,hash,username]);console.log('[ADMIN] Ready:',q.rows[0]);}finally{await pool.end();}})().catch(err=>{console.error('[ADMIN] Failed:',err.message);process.exit(1);});
